import React from 'react';

// Source-attribution chips ("Based on: …") are intentionally hidden — product
// decision. The backend still returns and persists `sources`/attributions with
// every answer, so re-enabling this display later only means restoring this
// component's previous rendering (see git history).
export default function SourceList() {
  return null;
}
